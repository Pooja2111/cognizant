import { Pooja5Directive } from './pooja5.directive';

describe('Pooja5Directive', () => {
  it('should create an instance', () => {
    const directive = new Pooja5Directive();
    expect(directive).toBeTruthy();
  });
});
